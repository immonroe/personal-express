<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Purr-fection</title>
  <link rel="shortcut icon" href="favicon.ico">
  <link rel="stylesheet" href="font-awesome.min.css">
  <link rel="stylesheet" href="styles.css">
</head>
<body>
  <h1>Purr-fection - A Cat Lover's Paradise</h1>
  <h2>Let's all share the joy of cats - using fullstack technology so everyone is always seeing the same thing!</h2>

  <h3>Meow</h3>
  <div class="cat-container">
    <% if (catImageUrl) { %>
      <img src="<%= catImageUrl %>" alt="Cat image" id="catImage" />
    <% } else { %>
      <p id="message">No cat image yet! Hit the button to generate one.</p>
    <% } %>
  </div>

  <div class="buttons">
    <button id="generateCat">
      <i class="fa fa-refresh"></i> Generate New Cat
    </button>
  </div>

  <script src="main.js"></script>

</body>
</html>
